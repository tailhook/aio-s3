containers:

  test:
    builder: arch
    parameters:
      packages: python-pip
    provision: |
      pip install aiohttp

commands:

  check:
    container: test
    command:
    - python3
    - test.py
